{
  "name": "verdict",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "demo": "npm --prefix server run demo",
    "test": "npm --prefix server run test:swarm",
    "check:chunks": "npm --prefix server run check:chunks",
    "check:links": "npm --prefix server run check:links",
    "check:scores": "npm --prefix server run check:scores"
  }
}
